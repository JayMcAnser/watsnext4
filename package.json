{
  "scripts": {
    "import:wiki": "node api/job.js import:wiki -f wikipedia/artist.csv -r -d",
    "wikipedia": "node api/job.js generate:wikipedia -d",
    "watsnext": "node api/job.js import:watsnext -o agent.log -r --parts agent"
  }
}
